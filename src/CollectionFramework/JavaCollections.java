package CollectionFramework;

public class JavaCollections {
    public static void main(String args[]){

        /**
         *  Common Methods
         *  add(), get(), size(), contains(), isEmpty(), indexOf(), removeAll(), removeIf(), toArray(), clear()
         *
         */
    }
}
